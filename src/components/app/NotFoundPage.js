const style = {
  notFoundPageWrapper:
    "w-full my-4 text-2xl font-extrabold items-center text-center",
};

const NotFoundPage = () => {
  return (
    <div className={style.notFoundPageWrapper}>
      <h1>Not found</h1>
    </div>
  );
};

export default NotFoundPage;
